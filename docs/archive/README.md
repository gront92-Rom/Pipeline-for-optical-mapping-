# docs/archive/

Эти файлы — ревью **старого** пайплайна (`optical_pipeline_worker.py`, 3066 строк, монолит).
Они описывают баги F1-F42, SEV1-SEV4, агенты `rsm_mask_worker_v2/v3`, `auto_mask.py`, `alternans_worker.py` и т.д.

**Эти файлы НЕ относятся к cardiac_pipeline_v3.** v3 — новая модульная архитектура (BaseAgent + config/default.yaml).

Сохранены для исторической справки. Не используйте для оценки текущего состояния v3.
Для актуального статуса см. `tracking/MODULE_STATUS.md` и `tracking/PROGRESS_TRACKER.md`.