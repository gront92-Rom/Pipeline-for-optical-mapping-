"""
cardiac_pipeline — Pipeline for cardiac optical mapping data processing.

Modules:
    base_agent: Base class for all pipeline agents
    utils.metadata_extractor: Read-only metadata extraction from MiCAM files
    utils.preprocess: Video preprocessing (spatial/temporal filtering, inversion)
"""
